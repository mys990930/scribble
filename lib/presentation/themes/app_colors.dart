import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const bg = Color(0xFF142232);
  static const surface = Color(0xFF35465C);

  // Main tri-color
  static const primary = Color(0xFFE6535E);
  static const secondary = Color(0xFFFCD364);
  static const tertiary = Color(0xFF6BBC8A);

  // Extra accents
  static const accentWarm = Color(0xFFF09661);
  static const accentCool = Color(0xFF58C3EC);
  static const accentPurple = Color(0xFF746DA7);

  // Neutrals
  static const white = Colors.white;
  static const black = Colors.black;
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFDCE3EA);
  static const textMuted = Color(0xFFAEB9C6);

  // Semantic
  static const overdue = primary;
  static const warning = secondary;
  static const success = tertiary;
}
